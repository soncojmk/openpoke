[phases.setup]
nixPkgs = ["python311", "python311Packages.pip"]

[phases.install]
cmds = ["pip install -r server/requirements.txt"]

[start]
cmd = "python -m server.server --host 0.0.0.0 --port ${PORT:-8001}"
